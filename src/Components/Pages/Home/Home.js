import { listData } from "../../../api";
import { Loading } from "../../Loading";

export const Home = () => {
  // const data = listData();
  // console.log(data);

  return (
    <div>
      <Loading />
    </div>
  );
};
